# Run log content-pipeline - 2026-07-27

## Bài P1: /pr-la-gi/ (post ID 4722)

| Bước | Làm/bỏ qua | Bằng chứng |
|---|---|---|
| Gate (trùng bài) | Đã kiểm `wp post list -s='PR'` trên live - không có bài `/pr-la-gi/` sẵn có; `/bai-pr-la-gi/` (4670) tồn tại nhưng khác góc độ (định dạng bài viết, không phải khái niệm ngành) - đã đọc H1/H2 live để xác nhận trước khi viết. | curl `/bai-pr-la-gi/` H2: "Định nghĩa và mục đích của bài PR", "Bài PR khác gì bài quảng cáo"... - xác nhận không trùng góc độ. |
| Research SERP | WebSearch "pr là gì quan hệ công chúng" + "quan hệ công chúng là gì vai trò PR marketing" (đọc danh sách top link + snippet tổng hợp). WebFetch 4/10 bài top thật: gobranding.com.vn, mona.media, sapo.vn, saokim.com.vn. | 4/4 bài fetch xác nhận dạng informational/định nghĩa (không phải listicle/trang dịch vụ) - độ dài 1.000-4.000 từ, saokim có mục Earned/Owned/Paid Media. |
| Allintitle | Không đo được số đếm chính xác - `WebSearch` không hỗ trợ toán tử `allintitle:` trả về con số, chỉ trả kết quả thường (topcv.vn, jobsgo.vn, mona.media, gobranding.com.vn, cellphones, seoviet.vn đều có "PR là gì" trong title) - ghi rõ KHÔNG bịa số, chỉ quan sát được nhiều đối thủ cùng cạnh tranh head-term này. | Kết quả WebSearch "allintitle:\"pr là gì\"" trả về danh sách bài thường, không có count. |
| Dàn bài + info gain | 9 H2: định nghĩa, phân biệt bài PR/thông cáo báo chí, lịch sử, vai trò marketing-mix, PR vs Marketing/QC (earned-owned-paid), kênh PR, xu hướng 2026, FAQ, kết luận. Info gain: gắn PR với dịch vụ booking đăng bài PR trên báo điện tử (đối thủ agency marketing thuần không có), box phân biệt rõ 3 khái niệm PR/bài PR/thông cáo báo chí (không đối thủ nào làm rõ 3 khái niệm cùng lúc). | Xem H2 list trong nội dung bài; box so sánh 3 khái niệm dùng HTML card. |
| Viết trực diện | Mỗi H2 mở đầu bằng câu trả lời thẳng (vd "PR là viết tắt của Public Relations..."). | Đọc đoạn đầu mỗi H2 trong file `pr-la-gi-content.html`. |
| Visual coverage | 2 ảnh Storyset (friendly-handshake/amico, journalist/amico - tải SVG gốc, recolor accent #BA68C8 -> #0E8C7F brand teal, xuất webp), 3 sơ đồ HTML (lịch sử PR 4 mốc, Earned/Owned/Paid 3 khối, kênh PR 5 thẻ), 1 bảng `.dgc-data-table`, 1 card-grid vai trò, 1 card-grid xu hướng, 1 box phân biệt 3 khái niệm, 1 chip-strip FAQ, 1 CTA box kết luận - đủ mọi H2 có >=1 visual. | curl live: `pr-la-gi-handshake-amico.webp` + `pr-la-gi-journalist-amico.webp` xuất hiện 2 lần mỗi ảnh (content + có id); `grep -c dgc-data-table` = 1; 9 H2 đối chiếu từng khối HTML trong file. |
| External link E-E-A-T | PRSA (https://www.prsa.org/about/all-about-pr, định nghĩa gốc, dofollow noopener), Wikipedia tiếng Việt (Quan hệ công chúng, dofollow noopener), Wikipedia Ivy Lee (lịch sử, dofollow noopener - không phải trích cá nhân nên không nofollow). | curl live grep 3 link, đều có `target="_blank" rel="noopener"`. |
| Widget | `[dgc_offpage_quiz]` chèn sau H2 thứ 3 (Lịch sử). | curl live: class `oquiz`, `oquiz-q`, `oquiz-result`... render đầy đủ, không lộ shortcode thô. |
| Backup trước sửa | Không refresh bài cũ (bài P1 là bài MỚI hoàn toàn) - không cần backup cho chính bài P1. | Ghi manifest "created" cho post 4722. |
| Đăng live | `wp post create` qua file tạm (SSH), post_status=publish, post_author=1, category booking-bao-pr (term 24). | Post ID 4722; curl `https://digicomvn.com/pr-la-gi/` = HTTP 200; `wp post term list 4722 category` = "Booking báo & PR". |
| Thumbnail | `tools/blog-thumbnail/render-illus.py` với post ID 4722 + tiêu đề, phân loại BOOKING PR/PR. | scp + `wp media import --post_id=4722 --featured_image` = attachment 4723 thành công; curl live có `thumb-4722.png` nhiều size. |
| Internal link 2 chiều | Trong bài mới: link `/bai-pr-la-gi/`, `/cach-viet-bai-pr-chuan-bao-chi/`, `/booking-bao-pr/` (money page, x2), `/agency-booking-bao-chi/`. Chiều ngược: post 4670 (`bai-pr-la-gi`) và post 1277 (`cach-viet-bai-pr-chuan-bao-chi`) thêm 1 câu + link trỏ `/pr-la-gi/` mỗi bài. | Backup content.raw tại `~/Claude-Workspace/_backups/routines/2026-07-27/pr-la-gi-inbound/{4670,1277}-*-before.html`; manifest.md ghi 3 dòng lúc 20:46; curl `bai-pr-la-gi` và `cach-viet-bai-pr-chuan-bao-chi` đều có `href="/pr-la-gi/"`. |
| GSC submit | URL Inspection qua Chrome (property URL-prefix `https://digicomvn.com/`, tài khoản đã đăng nhập sẵn hieudx3107@gmail.com), bấm "Yêu cầu lập chỉ mục" cho `/pr-la-gi/`. | Screenshot GSC hiện thông báo xanh "Đã yêu cầu lập chỉ mục". |
| Verify cuối | curl kiểm HTTP 200, 1 H1 đúng thứ tự H1->SAPO->Tóm tắt->mở bài, 0 em/en dash, quiz widget render, ảnh + bảng render. | Xem log lệnh curl trong phiên - toàn bộ PASS. |
| Log | Append LOG.md + cập nhật `content/cluster-pr-tongquat.md` (P1 -> Xong). | Xem 2 file, đã sửa trong phiên này. |

## Lưu ý phát hiện ngoài phạm vi (không tự sửa)
Theme `digicom-host` hiện KHÔNG output thẻ `<meta name="description">` hay Open Graph tags ở bất kỳ trang nào trên site (kiểm tra `header.php`, RankMath không active) - post_excerpt của bài mới đã điền sẵn 142 ký tự cho tương lai nhưng KHÔNG hiển thị ra HTML hiện tại. Đây là gap kỹ thuật toàn site có sẵn từ trước, ngoài phạm vi task viết bài này - cần Hiếu quyết định có làm riêng không.

## Bài B1: /backlink-audit/ (post ID 4739)

| Bước | Làm/bỏ qua | Bằng chứng |
|---|---|---|
| Gate (trùng bài) | `wp post list --s='backlink'` trên live - không có bài `/backlink-audit/` sẵn có. Bài gần nhất cùng chủ đề là `224 kiem-tra-backlink` (review công cụ) - đã curl live lấy H2 để xác nhận không trùng góc trước khi viết. | curl `/kiem-tra-backlink/` H2: "Top 5 công cụ...", "Cách đọc chỉ số...", "3 bước kiểm tra backlink xấu"... - xác nhận đó là bài công cụ, không phải quy trình audit. |
| Research SERP | WebSearch "backlink audit là gì cách audit backlink profile" + "how to audit backlink profile step by step". WebFetch 2 nguồn sâu: backlinko.com/step-by-step-backlink-audit (quy trình 5 bước + bảng 5 công cụ), ahrefs.com/blog/backlink-audit (Referring Domains, DR, anchor branded ratio, TLD, dấu hiệu spam). | Kết quả WebFetch tóm tắt trong phiên - cả 2 nguồn đều dạng hướng dẫn quy trình nhiều bước (khớp intent informational/how-to), không có ngưỡng số tuyệt đối cứng (tự tổng hợp thành bảng tương đối). |
| Allintitle | Không đo được số đếm chính xác - không có công cụ nào trong bộ MCP kết nối phiên này hỗ trợ toán tử `allintitle:` của Google trả về con số (đây là toán tử riêng của Google, không có trong API Ahrefs/Semrush). Ghi rõ KHÔNG bịa số. | Không có output số đếm nào được tạo ra hoặc dùng trong bài. |
| Dàn bài + info gain | 9 H2/7 H3: định nghĩa audit + backlink profile, 4 trigger cần audit, quy trình 7 bước, bảng 7 chỉ số kèm ngưỡng, dấu hiệu lành mạnh/khả nghi, công cụ free/trả phí, tự làm vs thuê dịch vụ, FAQ, kết luận. Info gain: bảng ngưỡng tổng hợp (224 và 2 nguồn quốc tế đều không có), phân vai rõ với bài công cụ đã có, số liệu anchor text dẫn nhất quán về bài `/ty-le-anchor-text-chuan/` thay vì bịa số mới. | Xem H2 list + bảng `.dgc-data-table` trong nội dung bài. |
| Viết trực diện | Mỗi H2 mở đầu bằng câu trả lời thẳng (vd "Backlink audit là quá trình phân tích toàn bộ backlink profile..."). | Đọc đoạn đầu mỗi H2 trong file `backlink-audit.html`. |
| Visual coverage | 2 ảnh Storyset (storyset-analysis.png, storyset-datareport.png - tái dùng ảnh đã có sẵn trên site, đúng chủ đề phân tích/báo cáo dữ liệu, đủ credit), 3 sơ đồ HTML (4 trigger card, quy trình 7 bước, so sánh 2 cột lành mạnh/khả nghi), 1 bảng `.dgc-data-table`, 1 chip-grid công cụ, 1 chip-grid FAQ - đủ mọi H2 có >=1 visual. | curl live: `storyset-analysis.png` + `storyset-datareport.png` xuất hiện đúng vị trí; `grep -c dgc-data-table` = 1; 9 H2 đối chiếu từng khối HTML. |
| External link E-E-A-T | Ahrefs blog (định nghĩa/phương pháp audit backlink, dofollow noopener - nguồn khái niệm SEO quốc tế, không phải trích cá nhân). | curl live grep link `ahrefs.com/blog/backlink-audit` có `target="_blank" rel="noopener"`. |
| Widget | `[dgc_offpage_quiz]` chèn sau H2 "Dấu Hiệu Backlink Profile Không Khoẻ Mạnh" (gần giữa bài, H2 thứ 5/9). | curl live: class `oquiz`, `oquiz-q`, `oquiz-result` render đầy đủ, không lộ shortcode thô. |
| Backup trước sửa | Bài B1 là bài MỚI hoàn toàn (không cần backup cho chính nó). 2 bài cũ bị sửa để thêm link ngược (224, 4226) đã backup content.raw trước khi update. | `~/Claude-Workspace/_backups/routines/2026-07-27/content-pipeline/224-kiem-tra-backlink-before.html`, `4226-co-nen-mua-backlink-before.html`. |
| Đăng live | `wp post create` qua file tạm (SSH), post_status=publish, post_author=1, category `backlink-offpage` (term 14). | Post ID 4739; curl `https://digicomvn.com/backlink-audit/` = HTTP 200. |
| Thumbnail | `tools/blog-thumbnail/render-illus.py` với post ID 4739 + tiêu đề, phân loại BACKLINK/BACKLINK. | scp + `wp media import --post_id=4739 --featured_image` = attachment 4740 thành công; `wp post meta get 4739 _thumbnail_id` = 4740. |
| Internal link 2 chiều | Trong bài mới (5, đúng giới hạn, mỗi URL 1 lần): `/dich-vu-backlink/` (money page), `/kiem-tra-backlink/`, `/co-nen-mua-backlink-khong/`, `/ty-le-anchor-text-chuan/`, `/backlink-dofollow-va-nofollow/`. Chiều ngược: bài 224 và 4226 thêm 1 câu + link trỏ `/backlink-audit/` mỗi bài. | curl live 3 URL đều chứa `backlink-audit` trong href; backup + update log SSH thành công (Success: Updated post 224/4226). |
| GSC submit | Thử mở URL Inspection qua Chrome (Claude_Browser), tab có sẵn origin accounts.google.com nhưng session KHÔNG đăng nhập (khác phiên trước cùng ngày) - hiện màn hình "Sign in" trống. KHÔNG nhập tài khoản/mật khẩu (rule bảo mật: không tự đăng nhập hộ). | Screenshot xác nhận màn hình Google Sign in trống, không có phiên nào đang mở. |
| Verify cuối | curl kiểm HTTP 200, 1 H1 đúng nội dung, 0 em/en dash, quiz widget + data-table render, thumbnail gắn đúng (_thumbnail_id). | Kết quả curl/python parse trong phiên - toàn bộ PASS trừ GSC. |
| Log | Append LOG.md + tạo `content/cluster-backlink-gap.md` (B1 Xong, B2-B5 chưa làm) + cập nhật dòng #6 trong `content/cluster-backlink.md` trỏ sang sổ cái mới. | Xem 3 file, đã sửa trong phiên này. |

## Bài B3: /disavow-backlink/ (post ID 4749)

| Bước | Làm/bỏ qua | Bằng chứng |
|---|---|---|
| Gate (trùng bài) | `wp post list -s='disavow'` + `-s='backlink xấu'` trên live - không có bài `/disavow-backlink/` sẵn có. Bài `224 kiem-tra-backlink` có sẵn mini-section 3 bước disavow rất ngắn - đã đọc nguyên đoạn (curl + parse) trước khi viết để xác định góc khác biệt. | Output `wp post list` không có slug `disavow-backlink`; đoạn text 224 đã trích trong phiên (mục "Bước 1/2/3" + 5 dấu hiệu). |
| Research SERP | WebSearch 3 truy vấn (disavow là gì, toxic backlink checker, how to disavow 2026). WebFetch trực tiếp support.google.com/webmasters/answer/2648487 (nguồn gốc, quan trọng nhất), ahrefs.com/blog/toxic-backlinks/ (quote Mueller + Haynes + case 60% traffic), seongon.com/blog/seo/disavow-link.html (cấu trúc đối thủ VN). | Nội dung trích dẫn nguyên văn từ 2 nguồn WebFetch đã đưa vào bài kèm link; seongon dùng để đối chiếu cấu trúc (5 mục, ảnh chụp màn hình). |
| Allintitle | Không đo được số đếm chính xác - không công cụ MCP nào hỗ trợ toán tử `allintitle:` trả số. Ghi rõ KHÔNG bịa số. | Không có số allintitle nào xuất hiện trong bài hoặc báo cáo. |
| Dàn bài + info gain | 10 H2/5 H3: định nghĩa toxic/spammy/manipulative, dấu hiệu nhận biết, Disavow Tool là gì/truy cập ở đâu, định dạng file .txt, quy trình 6 bước, NÊN/KHÔNG NÊN (cốt lõi bài), sai lầm thường gặp, tự làm/thuê, FAQ, tổng kết. Info gain: bảng dấu hiệu x rủi ro x hành động (đối thủ không phân tầng); trích nguyên văn điều kiện disavow của Google + case 60% traffic (đối thủ VN không nêu). | Xem H2 list + bảng `.dgc-data-table` + đoạn trích quote trong nội dung bài. |
| Viết trực diện | Mỗi H2 mở đầu bằng câu trả lời thẳng (vd "Disavow backlink là thao tác gửi cho Google..."). | Đọc đoạn đầu mỗi H2 trong file `disavow-backlink.html`. |
| Visual coverage | 2 ảnh Storyset tái dùng (storyset-agency-warning-1.webp ID 4618, analysis-rafiki.webp ID 4736 - đã xem trực tiếp ảnh trước khi chọn, khớp chủ đề cảnh báo/phân tích), 1 bảng `.dgc-data-table`, 1 sơ đồ quy trình 6 bước, 1 sơ đồ breadcrumb vị trí công cụ GSC, 1 code-block ví dụ file .txt, 1 bảng so sánh NÊN/KHÔNG NÊN, 2 chip-grid, 1 CTA-grid ở tổng kết - xác nhận bằng script Python đếm visual/H2, cả 10 H2 đều có. | Script `python3` đếm `dgc-data-table`/`wp-block-image`/`wp:html` theo từng H2 trong phiên - output "OK" cho cả 10/10 H2. |
| External link E-E-A-T | support.google.com/webmasters/answer/2648487 (dofollow noopener, chính sách gốc) x2; ahrefs.com/blog/toxic-backlinks/ (nofollow noopener, trích dẫn cá nhân Mueller/Haynes) x2. | curl live grep 2 domain, đúng rel tương ứng (`noopener` cho Google, `nofollow noopener` cho Ahrefs). |
| Widget | `[dgc_offpage_quiz]` chèn sau H2 thứ 2 (Dấu hiệu nhận biết). | curl live: class `oquiz` render, không lộ shortcode thô (`[dgc_offpage_quiz]` không xuất hiện dạng text). |
| Backup trước sửa | Bài B3 là bài MỚI hoàn toàn. 2 bài cũ sửa thêm link ngược (4739, 224) đã backup content.raw trước khi update. | `~/Claude-Workspace/_backups/routines/2026-07-27/content-pipeline-disavow/4739-backlink-audit-before.html`, `224-kiem-tra-backlink-before.html`. |
| Đăng live | Lần 1 `wp post create < file` KHÔNG nạp được content (content.raw chỉ 1 ký tự - lỗi thao tác, thiếu cờ đọc STDIN đúng cách của WP-CLI). Phát hiện qua bước verify (H1 count = 0), sửa bằng `wp post update 4749 <file>` (truyền file làm positional argument), verify lại content.raw = 36.377 ký tự mới tiếp tục. post_status=publish, post_author=1, category `backlink-offpage` (term 14, đã tự khớp sẵn không cần set lại). | Post ID 4749; `wp post get 4749 --field=content \| wc -c` = 36377 sau khi sửa; curl `https://digicomvn.com/disavow-backlink/` = HTTP 200. |
| Thumbnail | `tools/blog-thumbnail/render-illus.py` với post ID 4749 + tiêu đề, phân loại BACKLINK/BACKLINK. | scp + `wp media import --post_id=4749` (porcelain) = attachment 4750 + `wp post meta update 4749 _thumbnail_id 4750` thành công. |
| Internal link 2 chiều | Trong bài mới (5, đúng giới hạn, mỗi URL 1 lần, đã fix từ bản nháp có 2 link trùng): `/dich-vu-backlink/` (money page), `/backlink-audit/` (B1), `/co-nen-mua-backlink-khong/`, `/kiem-tra-backlink/`, `/google-penguin/`. Chiều ngược: bài 4739 (thay câu "sẽ có hướng dẫn riêng" bằng link thật) và 224 (thêm câu link sau đoạn tóm tắt 3 bước cũ) trỏ `/disavow-backlink/`. | Python Counter đếm href nội bộ trong phiên = đúng 5 unique + /lien-he/ riêng; curl `backlink-audit` và `kiem-tra-backlink` đều chứa `href="/disavow-backlink/"`. |
| GSC submit | Mở tab origin search.google.com có sẵn trong phiên trình duyệt chung (dùng lại tab từ agent khác) - redirect ra `accounts.google.com` / landing page marketing, không phải console thật, không có session đăng nhập. KHÔNG tự đăng nhập hộ (rule bảo mật). | `get_page_text` trả về nội dung landing page "Start now / Improve your performance..." - không phải dashboard Search Console thật. |
| Verify cuối | curl kiểm HTTP 200, 1 H1 đúng nội dung ("Disavow Backlink Là Gì?..."), 0 em/en dash, title tag đúng, widget/table/glossary render, 6 internal link (5 + /lien-he/) đều HTTP 200. | Script Python parse `/tmp/live-disavow.html` trong phiên - toàn bộ PASS trừ GSC; loop curl 6 URL = toàn bộ 200. |
| Log | Append LOG.md + cập nhật `content/cluster-backlink-gap.md` (B3 Xong, tổng 4/5) + cập nhật `content/cluster-backlink.md` (dòng #5 Xong, ghi chú cannibalization với dòng #4, chưa tự xoá). | Xem 3 file, đã sửa trong phiên này (re-read trước khi ghi do file bị agent khác sửa song song). |

## Lưu ý phát hiện ngoài phạm vi (không tự sửa)
Dòng #4 trong `content/cluster-backlink.md` ("Backlink Xấu (Toxic Backlink) Là Gì? Dấu Hiệu Và
Cách Xử Lý") giờ trùng lặp lớn về nội dung định nghĩa/dấu hiệu với bài B3 vừa đăng - đã ghi cảnh
báo cannibalization trong sổ cái, KHÔNG tự xoá/gộp, chờ Hiếu quyết định (rule content-pipeline
mục A2c yêu cầu chỉ đề xuất, không tự gộp).
